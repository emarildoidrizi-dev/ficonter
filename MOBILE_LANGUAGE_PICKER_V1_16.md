# FICONTER Mobile Language Picker V1.16

## Rule
The global header language selector must stay compact and readable on phones.

- Narrow dropdown instead of a large sheet.
- Shorter language rows.
- Native and English language names remain visible.
- Internal scrolling is used if the viewport cannot show every language at once.
- The menu never grows beyond the visible phone viewport.
- The Settings language selector remains unchanged.
