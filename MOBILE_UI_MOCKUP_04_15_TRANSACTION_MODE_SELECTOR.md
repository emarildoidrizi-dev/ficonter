# Mobile UI Mockup 04.15 — Transaction mode selector

- Replaced the dense mode-selection copy with a compact `Choose your mode` header.
- Each mode is now one clear tap row with a dedicated icon, short description, compact effort badge, and chevron.
- Removed the secondary structure pill and long explanatory copy from the selector.
- Active mode remains visually distinct without adding another text element.
- Uses FICONTER theme variables for borders, surfaces, text, gold and solid controls.
- Preserves the existing mode-change logic, saved preference behavior, recurring entries, shortcuts and transaction forms.
