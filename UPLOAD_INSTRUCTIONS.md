# Upload instructions

This is a **complete repository package**.

Upload all files and folders inside `ficonter-main/` to the root of the GitHub repository. Do not combine it with older FICONTER ZIP packages after deployment; doing so could overwrite consolidated files with earlier versions.

Use a preview branch first. The Vercel deployment must show **Ready** before merging to `main`.
